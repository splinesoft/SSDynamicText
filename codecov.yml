comment:
    layout: "header, changes, diff, sunburst"
coverage:
    ignore:
        - "SSDynamicText/*"
        - "Example/*"
        - "Applications/*"
    status:
        patch:
            default:
                target: 99%
        project:
            default:
                target: 99%
